{% extends 'base.html.twig' %}

{% block title %}Hello CategoriaController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello World! ✅</h1>

    {% for categoria in categorias %}
        <div> <a href="{{path('verArticulosPorCategoria',{id: categoria.id}) }}">{{ categoria.nombre }} 
        <a href="{{path('deleteCategoria',{id: categoria.id}) }}">(borrar)</a></div>
    {% endfor %}
</div>

<a href="{{path('addCategoria')}}">Nueva categoria</a>
{% endblock %}
